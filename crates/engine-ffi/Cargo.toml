[package]
name    = "engine-ffi"
version.workspace = true
edition.workspace = true

[lib]
# rlib is included so integration tests (tests/) can link against the crate.
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
engine-core = { path = "../engine-core" }
